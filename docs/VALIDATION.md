# 검증 기록

검증 실행 결과는 이 문서와 마스터 기획서에 누적합니다.

## 2026-09-14 구현 검증

- Node 24.19.0 / Windows / Next.js 16.3.5
- TypeScript 검사와 ESLint: 통과
- 운영 빌드: 통과, 홈 화면과 3개 서버 API 생성
- Playwright: 데스크톱·360px 모바일 14개 테스트 통과
- 브라우저 수동 확인: 데스크톱 4열/모바일 1열, 검색·라이브 필터·이미지 표시 확인, 모바일 가로 넘침 없음
- 운영 의존성 `npm audit --omit=dev`: 취약점 0개. 전체 설치 검사에는 개발 도구 관련 moderate 4개가 남아 있으며 강제 메이저 업그레이드는 적용하지 않았음
- 브라우저 정적 번들에서 `CHZZK_CLIENT_SECRET`, `CRON_SECRET`, `DATABASE_URL` 문자열이 포함되지 않음을 확인
- 최종 단위/DB 통합 테스트: 4개 파일, **31개 통과**
- 최종 E2E: **14개 통과** (데스크톱 7개, 360px 모바일 7개)
- 최종 타입 검사·린트·운영 빌드: 모두 통과
- [데스크톱 실행 화면](screenshots/desktop.png) · [모바일 실행 화면](screenshots/mobile.png)
- GitHub 업로드: `Candy0424/CHZZK-STREAM-EXPLORER-AI`, 최초 소스 커밋 `c8af0ea`. `main`이 `origin/main`을 추적하도록 연결 완료

### 해결한 개발 환경 문제

1. Windows 경로 `C#`의 `#` 문자를 Vite가 URL fragment로 해석해 모듈을 찾지 못함. 동일 소스를 임시 경로에 복사하고 Node와 Vite의 symlink 보존 옵션으로 실행하는 테스트 진입점을 추가함. 임시 경로 검증 후 정리하고 원본 폴더는 유지함.
2. Tailwind의 기본 CSS import를 해석할 때 특수문자 경로가 잘못 변환됨. import 해석이 필요 없는 Tailwind utilities/theme 지시문과 프로젝트 자체 CSS를 사용함.
3. API 배치 테스트가 하나의 Response 객체를 재사용해 두 번째 JSON 읽기에 실패함. 요청마다 새 Response를 만드는 모킹으로 수정함.

## 자동 검증 범위

- API: Client 헤더, 커서 인코딩, KST 파싱, 401/403/404/429/500, 제한 재시도, 응답 필드 손실, 채널 20개 배치와 누락 감지
- 동기화: 전체 순회, 중복 제거, 부분 실패 스냅샷 보존, 커서 반복, 페이지 한도, 빈 결과, 실행 중복, 쿨다운, 비밀값 오류 마스킹
- 조회: 상태 집계, 온라인 우선, 검색·필터·페이지, 즐겨찾기 빈 목록, 지연 기준, URL 검증
- DB: 실제 마이그레이션 적용, 온라인/오프라인 전환, 숨김 유지, 롤백, 종료 이력 보존 (PGlite PostgreSQL 엔진)
- E2E: 데스크톱/360px 모바일, 필터·검색·초기화, 저장 지속, 오프라인 비활성화, 성인 썸네일 블러, 샘플 안내, 실제 링크 popup(네트워크 모킹), 지연 안내, 쿼리·인증 검증

## 아직 실행하지 않은 검증

- 실제 Client 인증을 사용한 라이브 API 전체 순회
- 운영 PostgreSQL 연결과 여러 프로세스의 session advisory lock
- 실존 20개 이상 채널 상태 비교, 방송 시작/종료 반영 지연 측정
- 공개 호스팅과 예약 작업 장기 실행
- 오전/오후/야간 실제 사용 후기

실행하지 않은 항목을 성공으로 기록하지 않습니다. UI 예시는 가상 데이터입니다.
